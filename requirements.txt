# Pinned to versions actually installed together and exercised by this
# project's own test suite (see tests/) as of this rewrite. `sentence-
# transformers` pulls in `torch`, which was not fully installed in the
# sandbox this was built in (no network access to download it there) --
# its version below was confirmed to exist on PyPI, but the real embedding
# model download/inference was not exercised end-to-end. Everything else
# in this file was actually installed and tested together.
#
# Upgrade deliberately, not blindly: the LangChain/Chroma ecosystem ships
# breaking changes across versions fairly often.

fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32          # required by FastAPI for file uploads -- missing from the original project
python-dotenv==1.2.3

langchain-core==1.6.3
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-groq==1.1.3
langchain-huggingface==1.2.2
chromadb==1.5.9
sentence-transformers==6.0.1
pypdf==6.18.1
python-docx==1.2.0
chardet==7.6.0
groq==0.37.1

pydantic==2.13.5

# --- Test-only (not required to run the app) ---
# pytest, httpx, reportlab -- see requirements-dev.txt
